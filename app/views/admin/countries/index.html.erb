<h1><%= t("admin.country") %></h1>
<div class="form-group">
  <%= link_to new_admin_country_path, class: "btn btn-success" do %>
    <span class="fa fa-plus"></span><%= t("admin.countries.add_new") %>
  <% end %>
</div>

<div class="col-sm-3">
  <%= search_form_for [:admin, @q] do |f| %>
    <div class="form-group">
      <%= f.text_field :name_cont, class:"form-control", placeholder: t("admin.countries.look_for")%>
    </div>
    <%= f.submit t("admin.countries.search"), class:"btn btn-primary" %>
  <% end %>
</div>

<table class="table table-striped table-dark">
  <thead>
    <tr>
      <th><%= sort_link(@q, :name, t("admin.countries.name")) %></th>
    </tr>
    <tbody>
      <% @countries.each do |country| %>
        <tr>
          <td><%= country.name %></td>
          <td>
             <%= link_to t("edit"), edit_admin_country_path(country), class: "btn btn-info btn-xs" %>
          </td>
          <td>
             <%= link_to t("delete"), admin_country_path(country), method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-danger btn-xs" %>
          </td>
        </tr>
      <% end %>
    </tbody>
</table>

<%= paginate @countries %>